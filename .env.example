# --- Telegram (required) ---
TELEGRAM_TOKEN=123456:ABC-YourBotFatherToken
TELEGRAM_CHAT_ID=42          # owner chat — the only chat the bot ever answers

# --- LLM backend (required) ---
# LLM_BACKEND selects which <NAME>_* triple below is active.
# Switch at runtime with /backend. Any OpenAI-compatible endpoint works.
LLM_BACKEND=llamacpp

# local llama.cpp / Ollama (no cloud needed):
LLAMACPP_BASE_URL=http://localhost:8080/v1
LLAMACPP_MODEL=local
LLAMACPP_API_KEY=

# OpenCode Zen (free tier):
ZEN_BASE_URL=https://opencode.ai/zen/v1
ZEN_MODEL=deepseek-v4-flash-free
ZEN_API_KEY=

# NVIDIA NIM:
NIM_BASE_URL=https://integrate.api.nvidia.com/v1
NIM_MODEL=meta/llama-3.1-8b-instruct
NIM_API_KEY=

# --- SearXNG (optional, used by /search) ---
SEARXNG_URL=http://host.local:8888

# --- Tuning (optional; see SETTING_DEFAULTS in rss_bot.py) ---
DIGEST_HOUR=8
IGNORE_AFTER_H=12
TOP_N=30
MIN_SCORE=10
PROFILE_EXAMPLES=30
SCORE_BATCH=10
ARTICLE_MAX_AGE_DAYS=7
LLM_MIN_INTERVAL=3
LLM_MAX_RETRIES=4
